{% extends '::base_dashboard.html.twig' %}
{% block body %}
    <h3 class="text-center">Importer des {{ entities }}</h3>
    {{  form_start(form) }}
    <div class="col-sm-12 text-center">{{ form_row(form.submitFile) }}</div>
    <div>
        {{ form_widget(form.save, {'attr': {'class': 'btn-primary center-block'}} ) }}
    </div>
    {{ form_end(form) }}
{% endblock %}
