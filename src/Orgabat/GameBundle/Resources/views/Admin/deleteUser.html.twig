{% extends '::base_dashboard.html.twig' %}
{% block body %}
    <h3 class="text-center">Supprimer un utilisateur</h3>
    <p>Etes-vous sûr de vouloir supprimer l'utilisateur <strong>{{ user.username }}</strong> ?</p>
    <form method="post" action="{{ path('admin_remove_user', {'user_id': user.id}) }}">
        <a href="{{ path('default_admin_board') }}" class="btn btn btn-default">
            <i class="fa fa-chevron-left"></i> Annuler
        </a>
        <button type="submit" class="btn btn-danger">
            <i class="fa fa-trash-o"></i> Supprimer
        {{ form_rest(form) }}
        </button>
    </form>
{% endblock %}
