{% extends '::base_dashboard.html.twig' %}
{% block body %}
    <h3 class="text-center">Supprimer une classe</h3>
    <p>Etes-vous sûr de vouloir supprimer la classe <strong>{{ section.name }}</strong> ?</p>
    <form method="post" action="{{ path('admin_remove_section', {'section_id': section.id}) }}">
        <a href="{{ path('default_admin_board') }}" class="btn btn btn-default">
            <i class="fa fa-chevron-left"></i> Annuler
        </a>
        <button type="submit" class="btn btn-danger">
            <i class="fa fa-trash-o"></i> Supprimer
        {{ form_rest(form) }}
        </button>
    </form>
{% endblock %}
